server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sathiyaashwin/readmee.git
          search-paths:
          - '{application}'